import { makeStyles } from '@material-ui/core';

const useStyles = makeStyles((theme) => ({
  label: {
    color: '#A3CB38',
    // width: 100,
    fontWeight: 'bold',
    paddingLeft: 5,
  },
}));

export default useStyles;
